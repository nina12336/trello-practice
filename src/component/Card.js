import React from "react";
import List from "./List";

export const Card = () => {
  return (
    <div>
      {/* <li key={id}>{item}</li> */}
      <li>222</li>
      <button>輸入的內容</button>
    </div>
  );
};

export default Card;
